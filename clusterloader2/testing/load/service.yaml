apiVersion: v1
kind: Service
metadata:
  name: {{.Name}}
{{if eq (ModInt .Index 2) 1}}
  labels:
    service.kubernetes.io/service-proxy-name: abstract
{{end}}
spec:
  selector:
    svc: {{.Name}}
  ports:
  - port: 80
    targetPort: 80
